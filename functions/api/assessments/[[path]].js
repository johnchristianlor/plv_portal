const JSON_HEADERS = { 'content-type': 'application/json; charset=utf-8' };
function json(data, status = 200) { return new Response(JSON.stringify(data), { status, headers: JSON_HEADERS }); }
function envValue(env, ...names) { for (const name of names) if (env[name]) return env[name]; return ''; }
function bearer(request) { const auth = request.headers.get('authorization') || ''; const match = auth.match(/^Bearer\s+(.+)$/i); return match ? match[1].trim() : ''; }
function dbEndpoint(env) { const raw = envValue(env, 'TURSO_DATABASE_URL', 'LIBSQL_URL').trim(); if (!raw) throw new Error('Turso database URL is missing.'); let base = raw.replace(/\/$/, ''); if (base.startsWith('libsql://')) base = 'https://' + base.slice('libsql://'.length); if (base.startsWith('http://')) base = 'https://' + base.slice('http://'.length); return base + '/v2/pipeline'; }
function supabaseUrl(env) { return envValue(env, 'SUPABASE_URL', 'NEXT_PUBLIC_SUPABASE_URL').replace(/\/$/, ''); }
function supabaseAnonKey(env) { return envValue(env, 'SUPABASE_PUBLISHABLE_KEY', 'SUPABASE_ANON_KEY', 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY'); }
function arg(value) { if (value === null || value === undefined) return { type: 'null' }; if (Number.isInteger(value)) return { type: 'integer', value: String(value) }; if (typeof value === 'number') return { type: 'float', value }; return { type: 'text', value: String(value) }; }
async function turso(env, statements) { const token = envValue(env, 'TURSO_AUTH_TOKEN', 'LIBSQL_AUTH_TOKEN'); if (!token) throw new Error('Turso auth token is missing.'); const requests = statements.map(item => ({ type: 'execute', stmt: { sql: item.sql, args: (item.args || []).map(arg) } })); const response = await fetch(dbEndpoint(env), { method: 'POST', headers: { ...JSON_HEADERS, authorization: 'Bearer ' + token }, body: JSON.stringify({ requests }) }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error('Turso request failed.'); return (body.results || []).map((result) => { if (result.type === 'error') throw new Error('Turso statement failed.'); const exec = result.response && result.response.result ? result.response.result : result.result; return exec || { cols: [], rows: [], affected_row_count: 0 }; }); }
function rows(result) { const cols = (result.cols || []).map(col => col.name || col); return (result.rows || []).map(row => { const out = {}; row.forEach((cell, index) => { out[cols[index]] = cell && Object.prototype.hasOwnProperty.call(cell, 'value') ? cell.value : null; }); return out; }); }
function id(prefix = 'id') { const uuid = crypto.randomUUID ? crypto.randomUUID() : Math.random().toString(36).slice(2) + Date.now().toString(36); return prefix + '_' + uuid.replace(/-/g, ''); }
function clampText(value, max = 1000) { return String(value || '').trim().slice(0, max); }
function parseJson(value, fallback) { try { return value ? JSON.parse(value) : fallback; } catch { return fallback; } }
async function getSupabaseUser(env, token) { const supabaseUrl = envValue(env, 'SUPABASE_URL', 'NEXT_PUBLIC_SUPABASE_URL').replace(/\/$/, ''); const anonKey = envValue(env, 'SUPABASE_PUBLISHABLE_KEY', 'SUPABASE_ANON_KEY', 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY'); if (!supabaseUrl || !anonKey || !token) return null; const response = await fetch(supabaseUrl + '/auth/v1/user', { headers: { apikey: anonKey, authorization: 'Bearer ' + token } }); if (!response.ok) return null; return response.json(); }
async function findProfile(env, accessToken, authUser, role) { const supabaseUrl = envValue(env, 'SUPABASE_URL', 'NEXT_PUBLIC_SUPABASE_URL').replace(/\/$/, ''); const anonKey = envValue(env, 'SUPABASE_PUBLISHABLE_KEY', 'SUPABASE_ANON_KEY', 'NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY'); const serviceKey = envValue(env, 'SUPABASE_SERVICE_ROLE_KEY'); if (!supabaseUrl || !anonKey) return null; const apiKey = serviceKey || anonKey; const dbBearer = serviceKey || accessToken; const checks = [['uid', authUser.id], ['id', authUser.id], ['email', authUser.email]].filter(([, v]) => !!v); for (const [field, value] of checks) { const url = new URL(supabaseUrl + '/rest/v1/users'); url.searchParams.set('select', '*'); url.searchParams.set(field, 'eq.' + value); url.searchParams.set('limit', '1'); const response = await fetch(url.toString(), { headers: { apikey: apiKey, authorization: 'Bearer ' + dbBearer } }); if (!response.ok) continue; const data = await response.json(); const profile = (data || []).find(row => String(row.role || '').toLowerCase() === role); if (profile) return profile; } return null; }
async function supabaseRpc(env, name, payload, accessToken = '') { const url = supabaseUrl(env); const anonKey = supabaseAnonKey(env); if (!url || !anonKey) throw new Error('Supabase connection is missing.'); const serviceKey = envValue(env, 'SUPABASE_SERVICE_ROLE_KEY'); const response = await fetch(url + '/rest/v1/rpc/' + name, { method: 'POST', headers: { ...JSON_HEADERS, apikey: serviceKey || anonKey, authorization: 'Bearer ' + (serviceKey || accessToken || anonKey) }, body: JSON.stringify(payload || {}) }); const data = await response.json().catch(() => null); if (!response.ok) throw new Error((data && data.message) || 'Supabase RPC failed.'); return data; }
async function validateStudentSession(env, studentNo, sessionToken) { if (!studentNo || !sessionToken) return false; try { const data = await supabaseRpc(env, 'validate_student_session', { p_student_no: studentNo, p_session_token: sessionToken }); return data === true; } catch (_) { return false; } }
async function getStudentProfile(env, studentNo, sessionToken) { if (!studentNo || !sessionToken) return null; try { const data = await supabaseRpc(env, 'get_student_profile', { p_student_no: studentNo, p_session_token: sessionToken }); return data && typeof data === 'object' ? data : null; } catch (_) { return null; } }
async function requireRole(request, env, role) { const token = bearer(request); if (!token) return { error: json({ error: 'Please login again.' }, 401) }; const authUser = await getSupabaseUser(env, token); if (!authUser || !authUser.id) return { error: json({ error: 'Please login again.' }, 401) }; const profile = await findProfile(env, token, authUser, role); if (!profile) return { error: json({ error: 'Access denied.' }, 403) }; if (String(profile.status || '').toLowerCase() === 'inactive') return { error: json({ error: 'Account is inactive.' }, 403) }; return { token, authUser, profile }; }
async function requireStudent(request, env) { const token = bearer(request); if (token) return requireRole(request, env, 'student'); const studentNo = clampText(request.headers.get('x-student-no') || request.headers.get('x-studentno') || '', 80); const sessionToken = clampText(request.headers.get('x-student-session') || request.headers.get('x-session-token') || '', 200); if (!studentNo || !sessionToken) return { error: json({ error: 'Please login again.' }, 401) }; const valid = await validateStudentSession(env, studentNo, sessionToken); if (!valid) return { error: json({ error: 'Please login again.' }, 401) }; const profile = await getStudentProfile(env, studentNo, sessionToken); if (!profile) return { error: json({ error: 'Access denied.' }, 403) }; if (String(profile.status || '').toLowerCase() === 'inactive') return { error: json({ error: 'Account is inactive.' }, 403) }; return { token: sessionToken, authUser: { id: studentNo }, profile }; }
async function ensureSchema(env) { await turso(env, [ { sql: `create table if not exists assessments (id text primary key, title text not null, instructions text, subject_code text, section text, status text not null default 'draft', duration_minutes integer not null default 30, opens_at text, closes_at text, settings_json text, created_by text, created_at text not null, updated_at text not null)` }, { sql: `create table if not exists assessment_questions (id text primary key, assessment_id text not null, type text not null, prompt text not null, points integer not null default 1, answer_key text, choices_json text, category text, difficulty text, explanation text, order_no integer not null default 1, created_at text not null, foreign key (assessment_id) references assessments(id) on delete cascade)` }, { sql: `create table if not exists assessment_attempts (id text primary key, assessment_id text not null, student_no text not null, student_name text, student_uid text, status text not null, answers_json text, score real not null default 0, total_points real not null default 0, violations integer not null default 0, started_at text not null, submitted_at text, deadline_at text, created_at text not null, foreign key (assessment_id) references assessments(id) on delete cascade)` }, { sql: `create table if not exists assessment_incidents (id text primary key, attempt_id text, assessment_id text, student_no text, type text not null, details text, created_at text not null)` }, { sql: `create index if not exists idx_assessments_status_section on assessments(status, section)` }, { sql: `create index if not exists idx_questions_assessment_order on assessment_questions(assessment_id, order_no)` }, { sql: `create index if not exists idx_attempts_student on assessment_attempts(student_no, assessment_id, status)` }, { sql: `create index if not exists idx_incidents_attempt on assessment_incidents(attempt_id, created_at)` } ]); }
async function readBody(request) { return request.json().catch(() => ({})); }
function assessmentFromRow(row) { return { ...row, duration_minutes: Number(row.duration_minutes || 30), settings: parseJson(row.settings_json, {}) }; }
function questionFromRow(row, includeKey = false) { const q = { id: row.id, assessment_id: row.assessment_id, type: row.type, prompt: row.prompt, points: Number(row.points || 1), choices: parseJson(row.choices_json, []), category: row.category || '', difficulty: row.difficulty || '', explanation: row.explanation || '', order_no: Number(row.order_no || 1) }; if (includeKey) q.answer_key = row.answer_key || ''; return q; }
async function adminList(env) { const [list] = await turso(env, [{ sql: `select * from assessments order by created_at desc` }]); return json({ assessments: rows(list).map(assessmentFromRow) }); }
async function adminGet(env, assessmentId) { const [a, qs] = await turso(env, [{ sql: `select * from assessments where id = ? limit 1`, args: [assessmentId] }, { sql: `select * from assessment_questions where assessment_id = ? order by order_no`, args: [assessmentId] }]); const assessment = rows(a)[0]; if (!assessment) return json({ error: 'Assessment not found.' }, 404); return json({ assessment: assessmentFromRow(assessment), questions: rows(qs).map(row => questionFromRow(row, true)) }); }
async function adminSave(request, env, admin) { const body = await readBody(request); const now = new Date().toISOString(); const assessment = body.assessment || {}; const questions = Array.isArray(body.questions) ? body.questions.slice(0, 300) : []; const assessmentId = clampText(assessment.id, 80) || id('asm'); const title = clampText(assessment.title, 140); const subject = clampText(assessment.subject_code, 80); const section = clampText(assessment.section || 'ALL', 80); const status = ['draft', 'published', 'closed', 'archived'].includes(assessment.status) ? assessment.status : 'draft'; const duration = Math.max(1, Math.min(240, Number(assessment.duration_minutes || 30))); if (!title || !subject || !section) return json({ error: 'Complete the title, subject, and section.' }, 422); if (status === 'published' && !questions.length) return json({ error: 'Add at least one question before publishing.' }, 422); const statements = [{ sql: `insert into assessments (id,title,instructions,subject_code,section,status,duration_minutes,opens_at,closes_at,settings_json,created_by,created_at,updated_at) values (?,?,?,?,?,?,?,?,?,?,?,?,?) on conflict(id) do update set title=excluded.title,instructions=excluded.instructions,subject_code=excluded.subject_code,section=excluded.section,status=excluded.status,duration_minutes=excluded.duration_minutes,opens_at=excluded.opens_at,closes_at=excluded.closes_at,settings_json=excluded.settings_json,updated_at=excluded.updated_at`, args: [assessmentId, title, clampText(assessment.instructions, 5000), subject, section, status, duration, assessment.opens_at || null, assessment.closes_at || null, JSON.stringify(assessment.settings || {}), String(admin.id || admin.uid || admin.email || 'admin'), now, now] }, { sql: `delete from assessment_questions where assessment_id = ?`, args: [assessmentId] }]; questions.forEach((question, index) => { const type = ['multiple_choice', 'true_false', 'short_answer', 'essay'].includes(question.type) ? question.type : 'multiple_choice'; const choices = Array.isArray(question.choices) ? question.choices.map(v => clampText(v, 500)).filter(Boolean).slice(0, 12) : []; statements.push({ sql: `insert into assessment_questions (id,assessment_id,type,prompt,points,answer_key,choices_json,category,difficulty,explanation,order_no,created_at) values (?,?,?,?,?,?,?,?,?,?,?,?)`, args: [clampText(question.id, 80) || id('q'), assessmentId, type, clampText(question.prompt, 6000), Math.max(1, Math.min(100, Number(question.points || 1))), clampText(question.answer_key, 1000), JSON.stringify(choices), clampText(question.category, 100), clampText(question.difficulty, 50), clampText(question.explanation, 2000), index + 1, now] }); }); await turso(env, statements); return json({ ok: true, id: assessmentId }); }
async function adminDelete(request, env) { const body = await readBody(request); const assessmentId = clampText(body.id, 80); if (!assessmentId) return json({ error: 'Assessment ID is required.' }, 422); await turso(env, [{ sql: `delete from assessments where id = ?`, args: [assessmentId] }]); return json({ ok: true }); }
async function adminAttempts(env, assessmentId) { const [result] = await turso(env, [{ sql: `select * from assessment_attempts where assessment_id = ? order by submitted_at desc, started_at desc`, args: [assessmentId] }]); return json({ attempts: rows(result).map(row => ({ ...row, answers: parseJson(row.answers_json, {}), score: Number(row.score || 0), total_points: Number(row.total_points || 0), violations: Number(row.violations || 0) })) }); }
async function adminIncidents(env) { const [result] = await turso(env, [{ sql: `select * from assessment_incidents order by created_at desc limit 200` }]); return json({ incidents: rows(result) }); }
async function studentList(env, profile) { const section = String(profile.section || '').trim(); const studentNo = String(profile.studentNo || profile.student_no || profile.username || '').trim(); const [assessmentsResult, attemptsResult] = await turso(env, [{ sql: `select * from assessments where status in ('published','closed') and (section = 'ALL' or section = ?) order by created_at desc`, args: [section] }, { sql: `select * from assessment_attempts where student_no = ? order by started_at desc`, args: [studentNo] }]); return json({ assessments: rows(assessmentsResult).map(assessmentFromRow), attempts: rows(attemptsResult).map(row => ({ ...row, answers: undefined, answers_json: undefined, score: Number(row.score || 0), total_points: Number(row.total_points || 0), violations: Number(row.violations || 0) })) }); }
async function studentStart(request, env, profile) { const body = await readBody(request); const assessmentId = clampText(body.assessment_id, 80); const studentNo = String(profile.studentNo || profile.student_no || profile.username || '').trim(); const studentName = String(profile.studentName || profile.fullName || profile.name || profile.email || 'Student').trim(); if (!assessmentId || !studentNo) return json({ error: 'Assessment cannot be started.' }, 422); const [aResult, qResult, openAttempt] = await turso(env, [{ sql: `select * from assessments where id = ? limit 1`, args: [assessmentId] }, { sql: `select * from assessment_questions where assessment_id = ? order by order_no`, args: [assessmentId] }, { sql: `select * from assessment_attempts where assessment_id = ? and student_no = ? and status = 'started' order by started_at desc limit 1`, args: [assessmentId, studentNo] }]); const assessment = rows(aResult)[0]; if (!assessment || assessment.status !== 'published') return json({ error: 'Assessment is not available.' }, 404); const nowMs = Date.now(); if (assessment.opens_at && nowMs < Date.parse(assessment.opens_at)) return json({ error: 'Assessment has not opened yet.' }, 403); if (assessment.closes_at && nowMs > Date.parse(assessment.closes_at)) return json({ error: 'Assessment is already closed.' }, 403); const existing = rows(openAttempt)[0]; const now = new Date().toISOString(); const deadline = new Date(nowMs + Number(assessment.duration_minutes || 30) * 60000).toISOString(); let attemptId = existing && existing.id; if (!attemptId) { attemptId = id('att'); await turso(env, [{ sql: `insert into assessment_attempts (id,assessment_id,student_no,student_name,student_uid,status,answers_json,score,total_points,violations,started_at,deadline_at,created_at) values (?,?,?,?,?,?,?,?,?,?,?,?,?)`, args: [attemptId, assessmentId, studentNo, studentName, String(profile.uid || profile.id || ''), 'started', '{}', 0, 0, 0, now, deadline, now] }]); } return json({ attempt: { id: attemptId, deadline_at: existing?.deadline_at || deadline, started_at: existing?.started_at || now }, assessment: assessmentFromRow(assessment), questions: rows(qResult).map(row => questionFromRow(row, false)) }); }
function grade(questions, answers) { let score = 0; let total = 0; const detail = {}; for (const question of questions) { const points = Number(question.points || 1); total += points; const expected = String(question.answer_key || '').trim(); const actual = String(answers[question.id] || '').trim(); const essay = question.type === 'essay'; const correct = !essay && expected && expected.toLowerCase() === actual.toLowerCase(); const earned = correct ? points : 0; score += earned; detail[question.id] = { answer: actual, points: earned }; } return { score, total, detail }; }
async function studentSubmit(request, env, profile) { const body = await readBody(request); const attemptId = clampText(body.attempt_id, 80); const answers = body.answers && typeof body.answers === 'object' ? body.answers : {}; const studentNo = String(profile.studentNo || profile.student_no || profile.username || '').trim(); const [attemptResult] = await turso(env, [{ sql: `select * from assessment_attempts where id = ? and student_no = ? limit 1`, args: [attemptId, studentNo] }]); const attempt = rows(attemptResult)[0]; if (!attempt) return json({ error: 'Attempt not found.' }, 404); if (attempt.status === 'submitted') return json({ ok: true, alreadySubmitted: true }); const [questionsResult] = await turso(env, [{ sql: `select * from assessment_questions where assessment_id = ? order by order_no`, args: [attempt.assessment_id] }]); const questions = rows(questionsResult).map(row => questionFromRow(row, true)); const result = grade(questions, answers); const now = new Date().toISOString(); await turso(env, [{ sql: `update assessment_attempts set status = 'submitted', answers_json = ?, score = ?, total_points = ?, submitted_at = ? where id = ? and student_no = ?`, args: [JSON.stringify({ raw: answers, graded: result.detail }), result.score, result.total, now, attemptId, studentNo] }]); return json({ ok: true, score: result.score, total_points: result.total, submitted_at: now }); }
async function studentIncident(request, env, profile) { const body = await readBody(request); const attemptId = clampText(body.attempt_id, 80); const type = clampText(body.type, 80) || 'unknown'; const details = clampText(body.details, 1000); const studentNo = String(profile.studentNo || profile.student_no || profile.username || '').trim(); const [attemptResult] = await turso(env, [{ sql: `select * from assessment_attempts where id = ? and student_no = ? limit 1`, args: [attemptId, studentNo] }]); const attempt = rows(attemptResult)[0]; if (!attempt) return json({ error: 'Attempt not found.' }, 404); const now = new Date().toISOString(); const newCount = Number(attempt.violations || 0) + 1; await turso(env, [{ sql: `insert into assessment_incidents (id,attempt_id,assessment_id,student_no,type,details,created_at) values (?,?,?,?,?,?,?)`, args: [id('inc'), attemptId, attempt.assessment_id, studentNo, type, details, now] }, { sql: `update assessment_attempts set violations = ? where id = ?`, args: [newCount, attemptId] }]); return json({ ok: true, violations: newCount }); }
export async function onRequest({ request, env, params }) { try { await ensureSchema(env); const rawPath = params.path; const path = '/' + (Array.isArray(rawPath) ? rawPath.join('/') : String(rawPath || '')); const url = new URL(request.url); if (path.startsWith('/admin')) { const auth = await requireRole(request, env, 'admin'); if (auth.error) return auth.error; if (request.method === 'GET' && path === '/admin/list') return adminList(env); if (request.method === 'GET' && path === '/admin/get') return adminGet(env, url.searchParams.get('id') || ''); if (request.method === 'POST' && path === '/admin/save') return adminSave(request, env, auth.profile); if (request.method === 'POST' && path === '/admin/delete') return adminDelete(request, env); if (request.method === 'GET' && path === '/admin/attempts') return adminAttempts(env, url.searchParams.get('assessment_id') || ''); if (request.method === 'GET' && path === '/admin/incidents') return adminIncidents(env); } if (path.startsWith('/student')) { const auth = await requireStudent(request, env); if (auth.error) return auth.error; if (request.method === 'GET' && path === '/student/list') return studentList(env, auth.profile); if (request.method === 'POST' && path === '/student/start') return studentStart(request, env, auth.profile); if (request.method === 'POST' && path === '/student/submit') return studentSubmit(request, env, auth.profile); if (request.method === 'POST' && path === '/student/incident') return studentIncident(request, env, auth.profile); } return json({ error: 'Assessment route not found.' }, 404); } catch (error) { return json({ error: error.message && error.message.includes('missing') ? error.message : 'Assessment service is temporarily unavailable.' }, 500); } }
